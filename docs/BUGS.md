# BUGS.md

# Padelstar – Active Bugs

Until the Monday 21 September 2026 milestone is achieved, bugs are prioritized by whether they block this chain:

**create account → log in → create Round Robin → start → register results → persist to Supabase → complete → create new tournament**

## P0 – Critical-path blockers

Add only reproducible blockers here.

### Account creation / login
- [x] Fixed and verified. Two issues found and resolved: (1) email deliverability — Supabase's shared built-in mailer had low sender reputation; fixed by configuring Resend as custom SMTP (domain `padelstar.app` verified, confirmed via Supabase auth_logs `mail.send` now going through Resend, email confirmed received). (2) Auth **Site URL** was still `http://localhost:3000` (a dev leftover) — meant every real signup on the live site hit a broken confirmation-link redirect. Fixed by setting Site URL to `https://padelstar.app` in Supabase Dashboard → Authentication → URL Configuration.
- [x] Account creation verified: `sigurd.grodem@live.no` — email received, link clicked, `email_confirmed_at` set in `auth.users`, session established (confirmed via auth_logs `user_signedup` + `login` events).
- [x] Login and auth-state-after-refresh verified — see "Authenticated (account-owned) path" below.

### Tournament creation
- [x] No known blocker. Was temporarily broken (create_tournament RPC 400, "column owner_profile_id does not exist") after an out-of-order migration deploy; fixed by applying the 3 missing chronological migrations (20260902233640, 20260904090000, 20260904210000) via Supabase SQL Editor. Verified working after fix.
- [x] New tournament creation verified (3x, guest mode): players, courts, Round robin format all persist to Supabase correctly.
- [x] Round Robin selection/setup verified.

### Tournament start
- [x] No known blocker. Round Robin generation verified: 8 players/2 courts produced correct doubles pairings, court assignment, round scheduling (7 rounds/42 matches total). Start persisted to Supabase and survived a full page reload.

### Result registration
- [x] No known blocker. Verified: registered all 6 Round 1 results via the score picker, court freed/reassigned correctly, round progressed to Round 2 automatically, 6/6 "Ferdig" count accurate.

### Server persistence
- [x] No known blocker. Verified via direct DB query: tournament creation and every result write persisted to Supabase (`updated_at`/state changes matched UI). Refresh/reopen restore verified: full page reload (fresh JS, no SPA state) correctly restored progress from the server.

### Tournament completion
- [x] No known blocker (fixed today, verified 2x end-to-end). Two bugs found and fixed: (1) "Fullfør turnering" crashed with `Cannot set properties of undefined (setting 'textContent')` because `app/bootstrap/dom-elements.js` never wired up `elements.confirmTitle` despite `#appConfirmTitle` existing in index.html — fixed in [app/bootstrap/dom-elements.js](../app/bootstrap/dom-elements.js) (cache-bust bumped to `-2`). (2) The `finalize_tournament` Postgres RPC it calls didn't exist on the live DB at all — deployed via migration. Finish flow confirmed working end-to-end: dialog → confirm → RPC 200 → guest tournament correctly deleted with a finalization receipt recorded (verified twice).
- [x] Final standings verified end-to-end on a full 3-round Round Robin tournament played to natural completion (see "Authenticated (account-owned) path" below for detail) — points, wins, sets, and games all correct.

### Start next tournament
- [x] No known blocker. Verified twice in a row: finish tournament → create new tournament → no stale player/setting carryover, persists correctly to Supabase, admin role correct.

### Authenticated (account-owned) path
- [x] Login verified: session established, survives full page refresh (checked via Profil page showing "Innlogget som"/"Logg ut").
- [x] Authenticated tournament creation verified: `owner_user_id` correctly bound to the logged-in account, `retention_expires_at` null at creation (not subject to guest 7-day expiry).
- [x] Authenticated tournament finish verified: record correctly **kept** (not deleted, unlike guest tournaments), status "Avsluttet", finalization receipt recorded.
- [x] Bug found, fixed, and verified. A pre-existing trigger `set_tournament_lifecycle_dates` (from migration 20260828121000, predates this session) used `coalesce(new.retention_expires_at, ...)` when a tournament transitions to "Avsluttet", which silently overwrote `finalize_tournament`'s explicit `retention_expires_at = null` for account-owned tournaments with a 30-day expiry. No data-loss risk occurred — `cleanup_expired_tournaments` only deletes rows where `owner_user_id is null`, so the wrongly-set date was never acted on — but the stored value was factually wrong. Fixed by guarding the trigger on `owner_user_id is null` (guest-path logic unchanged); the one already-affected row was corrected. Regression-verified with a fresh authenticated tournament: created → started → finished → `retention_expires_at` correctly `null`.
- [x] Fixed and verified. A logged-in account owner could not actually run a tournament: `admin_set_result`, `admin_advance_round`, `admin_advance_cup`, `admin_match_action`, `admin_undo_match`, and `delete_tournament` were all granted `EXECUTE` to the `anon` Postgres role only, never `authenticated` — confirmed via `has_function_privilege(...)` and reproduced live (registering a result while logged in failed with a 403 / `permission denied for function admin_set_result`, Postgres error 42501). All prior authenticated tests that session force-finished immediately without registering a result, which is why this was missed until now. The client correctly surfaced the failure rather than lying about success (DB confirmed: revision unchanged, match still "active", no score saved) — no data corruption, but the authenticated result-registration/round-advancement/match-action/undo/delete path was completely non-functional. Predates the session it was found in. Fixed by granting these 6 functions to `authenticated` (developer ran the SQL in Supabase Dashboard), matching the already-correct `create_tournament`/`finalize_tournament` grants and the functions' own security model (they authorize by admin_token possession inside the function body, not by Postgres role, so this only closed an access gap, not a new security surface). Verified via `has_function_privilege` returning true for all 6 functions, then end-to-end via a full authenticated-owner Round Robin playthrough: registered Round 1's result, advanced to Round 2, registered its result, advanced to Round 3, registered its result, finalized the tournament — every step persisted to Supabase correctly (revisions 2→7, confirmed by direct DB query after each action).
- [x] Verified. Natural Round Robin completion (play every round to its own end rather than force-finishing) and full-tournament standings were previously untested — now run end-to-end on a 4-player/1-court tournament (3 rounds, 1 match each). Each "Start neste runde" click correctly advanced the round server-side once all of the active round's matches reached `state: "finished"`, and final standings were verified mathematically correct against the 3 registered results (points, wins, sets, and games all tallied exactly). One harmless data inconsistency found along the way: `admin_set_result_impl` updates a finished match's `state` field to `"finished"` but never touches its separate `status` field, which stays at whatever it was before (e.g. `"active"`). This does not currently break anything — both `admin_advance_round_impl` (server) and `canCompleteRound`/`roundProgress` (client, in `app/tournament-status.js`) key off `state`, not `status` — but the two fields drifting out of sync is fragile groundwork for a future bug if any code path is ever added that trusts `status` instead. Fixed and verified. `supabase/migrations/20260915040000_admin_set_result_status_sync.sql` (adds the missing `status` writes alongside every existing `state` write in `admin_set_result_impl`, no other behavior change) applied by the developer in Supabase Dashboard SQL Editor. Verified directly: created a throwaway test tournament with a match in `state: "playing"`/`status: "active"`, called `admin_set_result_impl` with a winning score, and confirmed the returned match now has `state: "finished"` **and** `status: "completed"` together (previously `status` would have stayed stuck at `"active"`). Test row cleaned up via `finalize_tournament` afterward.

## P1 – Important but not Monday-blocking unless they break the critical path

### Cup format (docs/ROADMAP.md Phase 9)
- [x] Fixed and verified. Same class of bug as the Round Robin `admin_set_result_impl` status/state fix above, found while testing Cup end-to-end: `admin_advance_round_impl` (Round Robin) never updated a newly-activated match's `status` field when starting it on a court (confirmed against a real finished tournament: rounds 2/3's matches ended with `state: "finished"` but `status` still stuck at `"scheduled"`), and `admin_advance_cup_impl` built new bracket matches via `jsonb_build_object` without a `status` key at all (reads back as SQL `null`). Harmless today for the same reason as before — both functions and the client's `canCompleteRound`/`roundProgress` key off `state`, not `status`. Fixed via `supabase/migrations/20260915060000_advance_round_cup_status_sync.sql`; developer ran it, confirmed live (`pg_get_functiondef` on both functions now includes the `status` writes).
- [x] Verified end-to-end. Created an 8-player/4-team, third-place-match-enabled Cup tournament as the authenticated account owner: bracket generation (auto team pairing, 2-round bracket with pending final/third-place slots), semifinal round, `admin_advance_cup` advancement (correctly built the final from the two semifinal winners and the third-place match from the two losers), final + third-place result registration, automatic "Cup ferdig" completion with `cup.winnerTeam` set correctly (triggers inside `admin_set_result_impl` itself once the last match of the final round finishes — no separate advance click needed), and `finalize_tournament` (kept, not deleted, per the account-owned path). The admin's Kamper tab renders a complete round-by-round bracket view (`app/cup-bracket.js`) with per-match winners — this is fully wired up and working, contrary to an initial read of the code.
- [x] Fixed and verified. TV Mode (`tv.html`/`app/tv-mode.js`) showed only the generic win/points table for Cup tournaments, never the bracket or a declared winner. Added Cup-specific rendering to `app/tv-mode.js` (self-contained, no dependency on `app/cup-bracket.js`'s DI-based helpers since `tv-mode.js` is a separate standalone script): a real visual bracket-tree (rounds as columns, connecting lines between a pair's winner and their next match, drawn via an SVG overlay whose line coordinates are measured from the actual rendered card positions with `getBoundingClientRect` rather than assumed from CSS math — so it stays correct regardless of bracket depth or exact spacing), plus a "🏆 CUPMESTER" champion banner once `state.cup.winnerTeam` is set — following a developer request to match the visual style of a reference bracket graphic (NYT World Cup bracket). `get_spectator_tournament_by_code` already returned `state.cup` in full, so no server change was needed. Also made the whole TV layout adaptive per developer feedback ("should be modular... showing what is ideal for the current state"): whenever there are no live or queued matches (between rounds, or finished), the empty LIVE/NEXT panels collapse and the bracket (Cup) or standings table (Round Robin) expands to fill the freed width and height — applies to both formats, not just Cup. Added matching styles to `styles/tv.css` (`.tv-bracket-tree-wrap`, `.tv-bracket-svg`, `.tv-bracket-col`, `.tv-bracket-card`, `.tv-cup-champion`, `.tv-panel-full`) and a `.hidden` utility class that didn't previously exist there. Verified via a local static server (`.claude/launch.json`) against the live finished Cup Test Tournament at desktop (1600px) width — tree renders with correct connecting lines and winners at every round, champion banner shows "Player1 & Player2", panels correctly expand to fill the idle screen, no console errors. Regression-checked: the Round Robin "Natural Completion Test" tournament still shows its "STILLING" points table (now also full-width when idle) unchanged in content. Mobile/tablet fixed afterward: the ≤1050px and ≤700px media queries reserved fixed-height rows for the LIVE/NEXT panels regardless of whether they were hidden, so the idle-expanded panel got squeezed into (mobile) or overflowed past (desktop, at the time) far more height than its content needed. Fixed by toggling a `tv-idle` class on `.tv-columns` (set alongside the panel hide/show logic) that collapses those breakpoints' `grid-template-rows` down to a single flexible row when idle, and by switching `.tv-bracket-col` from `justify-content: space-around` to `center` with a `clamp()` gap so cards cluster together instead of spreading across whatever height happens to be available. Also made `.tv-bracket-tree-wrap` scrollable (`overflow: auto`, `.tv-bracket-col` `min-width: 130px`) with the SVG sized to the tree's actual scrollable content (`scrollWidth`/`scrollHeight`) rather than just the visible viewport, so a deeper bracket that doesn't fit narrow widths scrolls instead of squashing illegibly. Verified at 600px (mobile), 900px (tablet), and 1600px (desktop) widths against the live Cup Test Tournament — bracket clusters correctly with no wasted space or clipping at any width — and regression-checked Round Robin's standings table (also now correctly full-height when idle at all three widths). The old in-app spectator section (`index.html` `data-section="tournament"`) had the same gap at the time, superseded by TV Mode per the developer — since removed entirely (see the spectator-mode-removal entry below).

- [x] Fixed and verified. Non-Round-Robin tournament modes (Americano, Team-Americano, Mexicano, Team-Mexicano, King of the Court, Groups+Playoffs) were exposed in the format picker with real client-side scheduling logic (`app/tournament-modes.js`) but no working server-side round-advancement RPC (`admin_advance_round_impl` only accepts `roundRobin`). Removed the 6 broken `<option>`s from the format `<select>` in [index.html:447](../index.html) (client-side logic untouched, so re-enabling later is just adding the options back once each has a working RPC — see docs/ROADMAP.md Priority 2). Verified in browser: picker now only offers Round robin/Cup, no console errors.
- [x] Partially fixed; needs a real-device retest. Reported symptom: already-installed state isn't detected (install UI still shows as if not installed). Code review of `app/pwa-install.js` found the core detection (`matchMedia("(display-mode: standalone)")` + `navigator.standalone`) is the standard, correct approach, and there's no duplicate/out-of-sync install-state check elsewhere in the codebase. Cleaned up a dead condition (`!navigatorRef.userAgent`, which is never true in a real browser and had no effect) and added a defensive `?.` on the `matchMedia` result, plus a live `change` listener so the button reacts if display-mode flips without a reload. Could not reproduce the original symptom myself (browser automation can't simulate an actual "Add to Home Screen" install) — please retest on a real device and report back if it still shows incorrectly.
- [x] Fixed and verified. `index.html`/`guide.html`/`privacy.html` header nav made consistent: guide.html and privacy.html now have the same hamburger menu button/drawer as the main app (Hjem/Bli med/Opprett/Konto), instead of just a plain back-link. Required adding `?view=<module>` support to `app/initial-view.js` (whitelisted to landing/setup-player/setup-admin/account) so the links actually open the right section. Along the way found and fixed a real pre-existing bug: `app/guide-i18n.js` was blindly setting `textContent` on every `[data-guide-i18n]` element, which destroyed the logo `<img>` inside the back-link (replaced it with text) — fixed by adding proper `aria-label`-only handling for that element, matching the pattern `privacy-i18n.js` already used correctly. Also fixed the menu dropdown rendering in the wrong place (`.privacy-header` was missing `position: relative`, so the absolutely-positioned drawer anchored to the wrong ancestor). Follow-up fixes after developer feedback: (1) menu items were underlined (plain `<a>` default) unlike index.html's `<button>`-based menu — added `text-decoration: none` to `.app-menu .module-link` in `styles/layout.css` (a real cache-bust miss on my part: I'd edited the file without bumping its `?v=` query param, so the fix silently didn't load until caught and corrected); (2) logo on guide/privacy used a single flat `main_logo.png` instead of index.html's actual icon+wordmark lockup — replaced with the same `.brand-lockup`/`.brand-home-button` markup (icon + wordmark images) used on index.html; (3) menu was noticeably taller than index.html's — root cause was that `base.css`'s global `button { line-height: 1.3; }` rule applies to index.html's real `<button class="module-link">` elements but not to guide/privacy's `<a class="module-link">` (anchors need real hrefs to navigate), which fell back to an inherited `line-height: 1.5` instead, making each item ~3px taller. Fixed by setting `line-height: 1.3` explicitly on `.app-menu .module-link` in `styles/layout.css` so it's consistent regardless of tag. Verified in browser on both pages: menu opens/closes, logo and menu sizing/styling match index.html pixel-for-pixel (measured via computed styles, not just eyeballed), links navigate correctly, no console errors.
- [x] Fixed and verified. Two requested changes: (1) version number now has a single source of truth — `APP_VERSION` constant in `app/bootstrap/app-meta.js`, synced into the DOM the same way `syncCopyrightYear` already works (new `syncAppVersion`), replacing the old setup where the version lived only in `app/translations.js`'s Norwegian block (relied on accidental fallback for other languages) with a separate, already-stale hardcoded "v. 0.6" in `index.html`'s static markup. (2) Removed `text-transform: uppercase` from `body[data-theme="classic"] .version` in `styles/ui-consistency.css`, which was forcing the correct lowercase "v. 0.5.0" string to render as "V. 0.5.0". Verified in browser: footer now shows "v. 0.5.0" in lowercase, sourced from one constant.
- [x] Fixed and verified (two passes). Unwanted blue focus border on the page heading after load/module switch. `app/ui-effects.js`'s `focusModuleHeading()` deliberately focuses the active module's `h1`/`h2` via `tabindex="-1"` for screen readers on every `showModule()` call; `styles/ui-consistency.css` already had a rule meant to quiet the resulting default outline. Pass 1 fixed the rule's scoping (it was `body:not(.landing-active)`, inherited from this stylesheet's unrelated file-wide "landing excluded from surface rules" convention, so landing's own hero heading was the one place still showing it) but kept the original `:not(:focus-visible)` guard. Developer reported the border still showing on Styring after that — pass 2 found why: the guard depends on `:focus-visible`, and WebKit/Safari's heuristic treats a scripted `.focus()` call as "visible" far more often than Chromium's does, so the outline kept showing there regardless of page. Fixed by dropping the `:focus-visible` guard entirely — `outline: none` now applies unconditionally to `h1[tabindex="-1"]:focus, h2[tabindex="-1"]:focus`, which is safe specifically because tabindex="-1" elements are never in the real Tab order, so no sighted keyboard user can land on one and lose an indicator they needed. Verified: after creating a tournament and landing on Styring, `document.activeElement` is `h2#tournamentTitle` with computed `outline-style`/`box-shadow` both `none`, unconditionally (not contingent on `:focus-visible` matching this time). `npm test` 213/218 throughout (same 4 pre-existing unrelated failures).
- [x] Fixed and verified. PWA install icon showed the old gold/brown shield logo (or, for the maskable variant depending on the OS mask shape applied, something unrecognizable). `manifest.webmanifest`'s two icons (`padelstar-192.png` purpose "any", `padelstar-maskable-512.png` purpose "maskable" — what OS install prompts and home-screen icons actually render from) were never updated when the app's branding moved to the current blue shield; `index.html`'s own favicon/apple-touch-icon already used the current `assets/icons/padelstar-icon.png`, so the manifest was the one place the old icon survived. The maskable one had a second, separate problem: its artwork filled ~95% of the canvas edge-to-edge, far outside the maskable-icon spec's 80%-diameter safe-zone circle that OS-applied masks (circle/squircle/rounded-square) crop to — depending on the mask shape, a large chunk or most of the shield could get cut away, plausibly explaining "or nothing". Regenerated both from the current `padelstar-icon.png` (Pillow): `padelstar-192.png` is a straight high-quality resize; `padelstar-maskable-512.png` is composited onto an opaque 512×512 canvas filled with the manifest's own `background_color` (`#07111f`) with the logo scaled to sit inside the safe zone. Verified by measuring the maskable icon's actual non-background pixel radius from center (164.9px) against the spec's safe-zone radius (204.8px) — comfortably inside, not just eyeballed.
- [x] Fixed and verified. Tournament backup restore could leave the app in the old pre-TV-Mode spectator view instead of the admin workspace. Reproduced by intercepting the actual exported `Blob` content: `exportBackup()` ran state through `sanitizeSharedState()` (`app/state-manager.js`), which deletes `adminToken`/`playerToken`/`selectedPlayerId`/`ownerUserId` — correct for the remote/shared-state payload (spectators must never see the admin token), wrong for a personal backup file, whose whole point is letting the same admin restore their own session. With `adminToken` gone, `isCurrentUserAdmin()` can never return true again after restore, so `normalizeModule("admin")` fell through to the legacy `"tournament"` spectator module. Fixed: `app/backup-format.js`'s `serialize()` no longer sanitizes — a backup now keeps the full state including admin identity (whoever holds the file can already act as admin today via the admin link, so this isn't a new exposure). Also fixed a second real bug hit while testing: `importBackup()`'s `finally` block wrote `event.currentTarget.value = ""`, but `currentTarget` is only valid during synchronous dispatch and was already `null` by the time the async `FileReader` "load" callback ran — threw an uncaught `TypeError` on every import, success or failure, and left the file input never reset. Fixed by capturing `event.target` (which does survive) once at the top of the function. Verified end-to-end: created a tournament, exported its backup, cleared `localStorage` (simulating a different device), created a throwaway tournament to reach Styring, imported the saved backup — admin workspace loaded correctly with the backed-up tournament's name/players, sync toast fired, no console error, file input cleared.
- [x] Fixed and verified. Removed the legacy in-app spectator module entirely, per explicit developer decision ("the old spectator mode can be completely removed since it is no longer a feature the app shall have") — superseded by TV Mode (`tv.html`), so the gap noted in the earlier Cup-bracket-on-TV entry above no longer applies. Deleted the `data-section="tournament"` section and its nav link from `index.html`; `app/module-routing.js`'s `fallbackTournamentModule()` now falls back to `"landing"` instead of a `"tournament"` module that no longer exists; `app/workspace-navigation.js`, `app/bootstrap/dom-elements.js`, `app/standings.js`, `app/court-queue.js`, and `app/match-list.js` (dropped `renderSpectatorMatches`/`createSpectatorMatchCard`) had their now-dead element references and render calls removed. The cross-device "spectator link" feature (`app/link-utils.js`'s `createSpectatorLink`, the admin share panel's "Kopier tilskuerlenke" button) and the `?spectate=CODE` URL handler (`app/initial-view.js`) were repointed at `tv.html` (a real `window.location.href` redirect) instead of the deleted in-app view — `tv.html`/`app/tv-mode.js` already independently reads its own `?spectate=` param, so no server change was needed. The "View as spectator" player action and the nav menu's "TV Mode" button (`toggleTvModeFromMenu`) were also updated to stop referencing the deleted module. Removed the module's exclusive CSS (`.spectator-score-*`, `.spectator-live-group`, and the `.tv-mode [data-section="tournament"]` grid layout that only ever rendered this module) from `styles/modules.css`/`styles/styles.css`, keeping the shared `.standings`/`.court-queue` rules intact. Updated/removed the now-obsolete assertions in `test/padelstar.test.js` and `test/pwa.test.js`; full suite verified (`node --test test/*.test.js`, 215/219 passing — same 3 pre-existing unrelated failures as before this change, 1 skipped). Manually verified in-browser: nav no longer shows a "Turnering" link, "TV Mode" navigates cleanly to `tv.html`, the admin share panel's spectator link now reads `tv.html?spectate=<code>` and opens the real TV view, and owner/player standings still render correctly.
- [x] Fixed and verified. Guest "Admin har lagt meg til" (existing-player claim) previously failed for every player, always. Found during Phase 3 QA testing. Reproduced precisely by patching `PadelstarRemoteRpc.call` in a live browser session to capture the real RPC request/response (not just the toast): clicking an already-added player's name on the join page (`app/player-list.js`'s `renderExistingPlayerList`, the "Admin har lagt meg til" flow) as a guest (not logged in) always calls the `join_tournament` RPC. Two layers of this RPC's guest-guard logic turned out to be broken, found and fixed one at a time.
  - **Layer 1, `join_tournament` itself** — whenever an unauthenticated (`auth.uid() is null`) caller's player name matched ANY existing roster entry, it unconditionally raised `"Player name already joined; sign in to rejoin"`, including the very first person claiming a name the admin pre-typed into the roster. Fixed via `supabase/migrations/20260916203214_guest_join_allows_admin_added_players.sql`, narrowing the guard to only block when a `player_sessions` row already exists for that player id. Applied by the developer; confirmed live via `pg_get_functiondef` that `join_tournament` now checks `player_sessions` existence rather than blanket-blocking on a name match.
  - **Layer 2, `join_tournament_authenticated_impl`** (called by `join_tournament` after layer 1's guard passes) — found during Phase 29 re-verification, *after* layer 1's fix was applied and confirmed live, when the exact same claim flow still failed end-to-end with a *different* Postgres error: `"Player name belongs to another session"`. Root cause: this function's own account-ownership guard, `not exists (select 1 from tournament_account_players where ... and user_id = uid)`, uses `user_id = uid`, and for a guest `uid` (`auth.uid()`) is `null` — in SQL, `x = null` is never `true` (three-valued logic), so `exists(...)` is always `false` and the guard's `not exists(...)` is always `true` whenever `existing_player is not null`. Every guest claiming any already-existing player hit this unconditionally, regardless of whether it was ever actually claimed by anyone — the two-layer guard needed *both* layers correct for the flow to work, and only layer 1 was fixed. Reproduced live: fresh guest browser tab, a tournament with one never-before-claimed admin-added player ("Carl"), still failed identically after layer 1's fix. Fixed via `supabase/migrations/20260918144920_fix_guest_claim_existing_player.sql`, scoping the account-ownership check to `uid is not null` (authenticated callers only) — guests are already correctly gated by layer 1's `player_sessions` check, so they should skip this account-linkage guard entirely rather than fail it vacuously. Applied by the developer.
  Verified end-to-end against the live database after both fixes: a fresh guest browser tab, invite code for a tournament with one never-before-claimed admin-added player, clicking "Admin har lagt meg til" → the player name now succeeds, confirmed by a direct query showing a new `player_sessions` row for that player id.
  Known remaining limitation not covered by either fix: since the RPC matches by name, two players sharing the exact same name (duplicate names are intentionally allowed elsewhere in this app) can't be individually distinguished by this claim flow — only the first name-match in the roster is ever reachable. Not reproduced/fixed in this pass; flagged here for awareness.
- [x] Fixed and verified. Two bugs found during Phase 4/6/7 QA testing. (1) The desktop workspace rail (the primary Styring/Kamper/Tabell/Spiller nav, `#workspaceRail`) never appeared after starting a tournament from the lobby — root cause: `renderRoleVisibility()`, called by the main `render()` pipeline on every state change, never told `app/workspace-rail.js` to re-sync its `.hidden` class; only the narrower `showModule()`/`activateAdminPanel()` wrappers did. A full page reload masked it (the bootstrap path happens to sync), which is likely why this survived earlier passes. Fixed by also calling `PadelstarWorkspaceRail.syncActiveState()` from `app/app.js`'s `renderRoleVisibility()` wrapper. Verified live: starting a tournament now makes the rail appear immediately, confirmed via `#workspaceRail`'s computed `hidden` class before/after the fix (no reload needed). (2) The "Set resultat" button on a match card stayed enabled (not `disabled`) once the match finished or was cancelled, unlike every sibling control (undo, cancel, walkover) which correctly follows this pattern — clicking it silently did nothing (`openSetScoreDialog()` already refuses finished/cancelled matches internally), with zero feedback to the admin. Fixed in `app/match-card.js` by adding the same `["finished", "cancelled"].includes(match.state) ? "disabled" : ""` condition already used on the other buttons. Verified: the button now reports `disabled: true` for a finished match, `false` for the active one.
- [x] Round 2+ matches start with no court assigned (found in the Phase 8 continuous pass). Reproduced on the live database with a 4-player/2-court Round Robin: after "Start neste runde", the server's `admin_advance_round_impl` sets the next round's match to `playing`/`active` but never sets `courtId`/`courtName` (round 1 is courted by the client's `activateRound`/`assignNextCourt`; every later round is advanced by this RPC once the tournament is server-connected). Visible effect: the match card reads "Ikke tildelt bane" and the court queue shows every court LEDIG while a match is being played; because `admin_set_result_impl` hands a freed court to the next waiting match by copying the finished match's court, the missing court then propagates to the end of the tournament. Scoring and progression still work — this is a correctness/visibility defect, not a blocker. **Fix written but not yet applied**: `supabase/migrations/20260918201324_advance_round_assigns_courts.sql` assigns started matches to the tournament's courts in order (the same order the client uses) and clears `queuePosition`, matching `assignNextCourt()`; the new jsonb expressions were checked read-only against the live row. **Applied and verified (2026-09-19)**: re-ran a 4-player/2-court Round Robin on the live database — after "Start neste runde" round 2's match shows "Bane 1" / `active`, and the stored match row carries `courtId`/`courtName`; test tournament cleaned up with `finalize_tournament`.
- [ ] Player scoring the opponent team's point may wedge the score sync queue (suspected from code reading, NOT yet reproduced on two devices). The scoring table enables "+" on both team rows for a player whose match it is (`app/match-card.js` `scoreboardRow`), and `awardTennisPoint` queues every tap via `queuePlayerScore`; the live `save_player_point_impl` only accepts a point for a team the caller belongs to ("Player is not part of this match"). `processPlayerScoreQueue` (`app/remote-player-score.js`) `break`s on error without dropping the rejected item, so it would be retried first on every later sync and block later points. The approved scoring model (one active scorer per match, see ROADMAP Phase 10) will replace this per-team permission, so fix it as part of Phase 10.
- [ ] Other v1 feature bugs.

## Bug completion rule

A bug is complete only when:

- [ ] Reproduced or clearly verified.
- [ ] Smallest safe fix implemented.
- [ ] Relevant behavior tested.
- [ ] Critical-path regression checked.
- [ ] Status updated.

Do not spend time producing long bug-analysis documents.
